# Noesis Ecommerce Store

## Project Structure

- **client/**: Frontend application (React + Vite).
- **server/**: Backend server (Node.js + Express).

## Getting Started

### Server
1. Navigate to `server` directory: `cd server`
2. Install dependencies (if not already): `npm install`
3. Start the server:
   - Development: `npm run dev`
   - Production: `npm start`

### Client
1. Navigate to `client` directory: `cd client`
2. Install dependencies (if not already): `npm install`
3. Start the development server: `npm run dev`

## Next Steps
- Share the HTML/CSS code for the pages.
- We will integrate the design into the React components.
- We will build the backend API to support the store functionalities.
