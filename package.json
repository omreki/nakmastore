{
  "name": "nakma-store",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix client",
    "dev": "npm run dev --prefix client"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.89.0",
    "dotenv": "^17.2.3"
  }
}
